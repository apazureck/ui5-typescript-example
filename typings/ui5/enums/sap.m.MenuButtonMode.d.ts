declare module 'sap/m/library' {
    /**
    * Different modes for a MenuButton (predefined types).
    */
    export type MenuButtonMode = "Regular" | "Split";
    export namespace MenuButtonMode {
        const Regular: MenuButtonMode;
        const Split: MenuButtonMode;
    }
}