declare module 'sap/ui/core/library' {
    /**
    * Defines the accessible roles for ARIA support. This enumeration is used with the AccessibleRole control property. For more information, goto "Roles for Accessible Rich Internet Applications (WAI-ARIA Roles)" at the www.w3.org homepage.
    */
    export type AccessibleRole = "Alert" | "AlertDialog" | "Application" | "Banner" | "Button" | "Checkbox" | "ColumnHeader" | "Combobox" | "ContentInfo" | "Definition" | "Description" | "Dialog" | "Directory" | "Document" | "Grid" | "GridCell" | "Group" | "Heading" | "Img" | "Link" | "List" | "Listbox" | "ListItem" | "Log" | "Main" | "Marquee" | "Menu" | "Menubar" | "MenuItem" | "MenuItemCheckbox" | "MenuItemRadio" | "Navigation" | "Note" | "Option" | "Presentation" | "ProgressBar" | "Radio" | "RadioGroup" | "Region" | "Row" | "RowHeader" | "Search" | "Secondary" | "SeeAlso" | "Separator" | "Slider" | "SpinButton" | "Status" | "Tab" | "Tablist" | "Tabpanel" | "Textbox" | "Timer" | "Toolbar" | "Tooltip" | "Tree" | "TreeGrid" | "TreeItem";
    export namespace AccessibleRole {
        const Alert: AccessibleRole;
        const AlertDialog: AccessibleRole;
        const Application: AccessibleRole;
        const Banner: AccessibleRole;
        const Button: AccessibleRole;
        const Checkbox: AccessibleRole;
        const ColumnHeader: AccessibleRole;
        const Combobox: AccessibleRole;
        const ContentInfo: AccessibleRole;
        const Definition: AccessibleRole;
        const Description: AccessibleRole;
        const Dialog: AccessibleRole;
        const Directory: AccessibleRole;
        const Document: AccessibleRole;
        const Grid: AccessibleRole;
        const GridCell: AccessibleRole;
        const Group: AccessibleRole;
        const Heading: AccessibleRole;
        const Img: AccessibleRole;
        const Link: AccessibleRole;
        const List: AccessibleRole;
        const Listbox: AccessibleRole;
        const ListItem: AccessibleRole;
        const Log: AccessibleRole;
        const Main: AccessibleRole;
        const Marquee: AccessibleRole;
        const Menu: AccessibleRole;
        const Menubar: AccessibleRole;
        const MenuItem: AccessibleRole;
        const MenuItemCheckbox: AccessibleRole;
        const MenuItemRadio: AccessibleRole;
        const Navigation: AccessibleRole;
        const Note: AccessibleRole;
        const Option: AccessibleRole;
        const Presentation: AccessibleRole;
        const ProgressBar: AccessibleRole;
        const Radio: AccessibleRole;
        const RadioGroup: AccessibleRole;
        const Region: AccessibleRole;
        const Row: AccessibleRole;
        const RowHeader: AccessibleRole;
        const Search: AccessibleRole;
        const Secondary: AccessibleRole;
        const SeeAlso: AccessibleRole;
        const Separator: AccessibleRole;
        const Slider: AccessibleRole;
        const SpinButton: AccessibleRole;
        const Status: AccessibleRole;
        const Tab: AccessibleRole;
        const Tablist: AccessibleRole;
        const Tabpanel: AccessibleRole;
        const Textbox: AccessibleRole;
        const Timer: AccessibleRole;
        const Toolbar: AccessibleRole;
        const Tooltip: AccessibleRole;
        const Tree: AccessibleRole;
        const TreeGrid: AccessibleRole;
        const TreeItem: AccessibleRole;
    }
}